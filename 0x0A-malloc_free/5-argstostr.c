#include "holberton.h"
#include <stdlib.h>
/**
 *
 *
 *
 */
char *argstostr(int ac, char **av)
{
	char *bigstr;

	if ((ac == 0) || (av == NULL))
		return (NULL);
	/* determine bytes to be malloc'ed */
	myLen = (ac + 1 + total_len);
	if ( )

	
}
