#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

main()
{
	const char *myString = "\"Programming is like building a multilingual puzzle";

	puts(myString);
	return (0); 
}
